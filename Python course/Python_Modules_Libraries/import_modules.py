# Importing Modules from the Library

import random  # like opening the "dice app" on your phone

# Example: picking a random number like rolling a dice
num = random.randint(1, 6)
print(f"You rolled a {num} on the dice!")
